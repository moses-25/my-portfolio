// Compatibility wrapper: re-export the ParticleBackground default export
// Keeps existing imports (import ParticlesContainer from "../ParticlesContainer") working.
import ParticleBackground from "./ParticleBackground";

export default ParticleBackground;